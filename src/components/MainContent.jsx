import React from "react";
import "./MainContent.css"; // Import the CSS file

const MainContent = () => {
  return (
    <div className="main-content">
      <p>You have currently no events created.</p>
      <a href="#" className="start-link">Start here →</a>
    </div>
  );
};

export default MainContent;
